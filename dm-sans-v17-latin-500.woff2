/* ===== Footer ===== */
.footer {
  width: 100%;
  background: var(--dark);
  color: var(--light);
  padding: 64px 0 34px;
  margin-top: 80px;
}

.footer-top {
  display: grid;
  grid-template-columns: 200px 1fr 352px;
  gap: 40px;
  align-items: start;
}

.footer-left {
  display: flex;
  flex-direction: column;
  gap: 40px;
}

.footer-big-title {
  font-weight: 400;
  font-size: 56px;
  line-height: 60px;
  letter-spacing: -0.02em;
  margin: 0;
}

.social.social--footer {
  display: flex;
  gap: 12px;
  list-style: none;
  padding: 0;
  margin: 0;
}

.social.social--footer a {
  width: 44px;
  height: 44px;
  border-radius: var(--r10);
  border: 1px solid rgba(244, 244, 244, 0.1);
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 0.8;
  transition: opacity .2s;
}

.social.social--footer a:hover { opacity: 1; }

.social.social--footer img {
  width: 20px;
  height: 20px;
}

.footer-subscribe {
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.subscribe-title {
  font-size: 18px;
  line-height: 24px;
  margin: 0;
}

.subscribe-form {
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.subscribe-input,
.subscribe-btn {
  width: 100%;
  height: 48px;
  border-radius: var(--r30);
  box-sizing: border-box;
  font-size: 16px;
  line-height: 24px;
}

.subscribe-input {
  border: 1px solid var(--light);
  background: transparent;
  padding: 12px 14px;
  color: var(--light);
  outline: none;
}

.subscribe-input::placeholder {
  color: var(--light-60);
}

.subscribe-btn {
  border: none;
  background: var(--light);
  color: var(--text);
  cursor: pointer;
  transition: opacity .2s;
}

.subscribe-btn:hover { opacity: .85; }

.footer-bottom {
  margin-top: 34px; /* ↓ было 60, из-за этого высота */
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  gap: 16px;
}

.footer-copy {
  font-size: 14px;
  line-height: 18px;
  letter-spacing: -0.02em;
  color: #c6cdd1;
  opacity: .9;
  margin: 0;
}

.footer-links {
  display: flex;
  gap: 8px;
  font-size: 14px;
  line-height: 18px;
  color: #c6cdd1;
  align-items: center;
}

.footer-links a {
  color: #c6cdd1;
  text-decoration: none;
}

.footer-links a:hover { text-decoration: underline; }

/* =========================
   Modal + buttons (Exercise details / Rating)
   ========================= */

/* overlay */
/* =========================
   Modal (Exercise + Rating)
   ========================= */
.backdrop {
  position: fixed;
  inset: 0;
  background: rgba(4, 4, 4, 0.6);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20px;
  z-index: 2000;
  opacity: 1;
  transition: opacity 200ms ease;
}

.backdrop.is-hidden {
  opacity: 0;
  pointer-events: none;
}

/* modal shell */
.modal {
  position: relative;
  width: min(900px, 100%);
  max-height: calc(100vh - 40px);
  overflow: auto;

  background: #242424;
  color: #f4f4f4;

  border-radius: 20px;
  padding: 40px;

  box-shadow: 0 16px 60px rgba(0, 0, 0, 0.45);
}

@media (max-width: 575px) {
  .modal {
    width: min(335px, 100%);
    padding: 20px;
    border-radius: 20px;
  }
}

/* close button */
.modal-close {
  position: absolute;
  top: 14px;
  right: 14px;
  width: 28px;
  height: 28px;

  padding: 0;
  border: 0;
  border-radius: 8px;

  background: transparent url("../img/cross.svg") center/18px 18px no-repeat;
  color: transparent; /* hide × */
  cursor: pointer;

  opacity: 0.9;
  transition: opacity 200ms ease, transform 200ms ease, background-color 200ms ease;
}

.modal-close:hover {
  opacity: 1;
  transform: rotate(90deg);
  background-color: rgba(244, 244, 244, 0.08);
}

.modal-close:focus-visible {
  outline: 2px solid rgba(244, 244, 244, 0.35);
  outline-offset: 2px;
}

.modal-loading {
  margin: 0;
  color: rgba(244, 244, 244, 0.65);
}
